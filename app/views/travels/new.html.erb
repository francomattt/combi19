<% if user_signed_in? %>    
  <%if current_user.admin?%>
    <div class="container-fluid">
      <h3>Cargar nuevo viaje</h3>

      <%= render 'form', travel: @travel %>
	
      <%= link_to 'Volver', travels_path,class: 'btn btn-default btn-sm' %>
    </div>
  <%else%>
  <%end%>
<%end%>

