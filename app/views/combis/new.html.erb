<% if user_signed_in? %>    
  <%if current_user.admin?%>		
		<div class="container-fluid">
			<h3>Cargar nueva combi</h3>

			<%= render 'form', combi: @combi %>

			<%= link_to 'Volver', combis_path,class: 'btn btn-default btn-sm' %>
		</div>
	<%end%>
<%end%>