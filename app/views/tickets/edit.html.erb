<% if user_signed_in? %>    
  <%if current_user.passenger?%>
		<div class="container-fluid">
			<h3>Editar pasaje</h3>

			<%= render 'form', ticket: @ticket %>
			<div class="container">
			<br>
			</div>
			<%= link_to 'Ver ruta', @ticket, class: 'btn btn-primary btn-sm' %> | <%= link_to 'Volver', ways_path,class: 'btn btn-default btn-sm' %>
		</div>
	<%end%>
<%end%>
